# Required: IANA timezone used for calendar-day task calculations.
TIME_ZONE=Europe/London

# Optional. Defaults to 3001 in development and 3000 in production.
# PORT=3001

# Optional in development. Defaults to data/timesince.sqlite.
# Production requires an explicit absolute path outside the application release.
# DATABASE_PATH=data/timesince.sqlite
